import { IsdisabledDirective } from './isdisabled.directive';

describe('IsdisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new IsdisabledDirective();
    expect(directive).toBeTruthy();
  });
});
